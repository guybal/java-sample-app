apiVersion: v1
kind: Service
metadata:
  name: {{ include "drinks-app.fullname" . }}
  labels:
    {{- include "drinks-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "drinks-app.selectorLabels" . | nindent 4 }}

